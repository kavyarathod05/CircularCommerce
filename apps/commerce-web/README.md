# Commerce Web App

Target path for the React SPA. Currently deployed from `frontend/` at repo root.

```bash
cd frontend && npm install && npm run dev
```

Planned migration: `frontend/` → `apps/commerce-web/` (update `docker-compose.yml` build context when moved).
